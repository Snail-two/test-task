name: Java Ci

on:
  push:
    branches:
      - main
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          distribution: 'adopt'
          java-version: '11'

#      - name: Build with Maven
#        run: mvn install

      - name: Run test
        run: mvn test
